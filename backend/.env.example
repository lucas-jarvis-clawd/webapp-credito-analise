# Server
PORT=3001
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:5173

# JWT Authentication
# REQUIRED in production. In development, a default is used if not set.
JWT_SECRET=your_secure_random_secret_here
JWT_EXPIRES_IN=24h
